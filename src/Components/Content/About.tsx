import styled from "styled-components";

export const About = () => {
    return  (
        <div>
            <NavContainer>
                about works
            </NavContainer>
        </div>
    )
}

const NavContainer = styled.div`
    padding-left: 100px;
    padding: 10px;
    background-color:rgb(203, 219, 247);
`