import styled from "styled-components";

export const About = () => {
    return  (
        <div>
            <NavContainer>
                about works
            </NavContainer>
        </div>
    )
}

const NavContainer = styled.div`
    
`